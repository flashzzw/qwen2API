# 当前端独立部署（Vercel / 非单机模式）时，需指定后端地址
# 同时确保后端 .env 的 CORS_ALLOWED_ORIGINS 包含当前前端域名
# VITE_API_BASE_URL=https://your-backend.example.com

# 本地开发和 Docker 单机部署时留空，由 Vite proxy / nginx 代理
# VITE_API_BASE_URL=
